// constant (상수)
/* 
    1. 값이 한번 담기면 다른 값이 대입될 수 없다.
    2. 상수는 반드시 선언과 동시에 초기화 되어야한다.
*/

// const x;  // 값을 선언과 동시에 초기화 해야함

const x = 1;
// x = 2;  // error. 상수에는 초기값이 아닌 다른 값을 담을 수 없다.
console.log(x);

// let의 특징을 가지며 동시에 한 번만 값이 담기는 특징을 가지고 있다.